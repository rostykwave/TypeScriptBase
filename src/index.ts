//Debugging test
let age: number = 20;
if (age < 50) {
  age += 10;
}
console.log("age", age);

//Code completion
let numbers: number[] = [];
numbers.forEach((n) => n.toFixed);

//Tuple
const user: [number, string, boolean, number] = [1, "some", true, 1];
